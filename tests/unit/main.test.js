describe('Testing main function with fake tests', () => {
    test('First test', () => {
        expect(1).toEqual(1)
    })
    test('Second test', () => {
        expect(2).toEqual(2)
    })
    test('Third test', () => {
        expect(3).toEqual(3)
    })
});
